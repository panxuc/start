# 安全说明

## 支持版本

`main` 和 `master` 分支会接收安全修复。

## 报告安全问题

请不要在公开 Issue 中披露敏感漏洞。可以通过维护者邮箱联系，或在仓库启用 GitHub 私密漏洞报告后使用该功能。

报告时请尽量包含：

- 受影响的提交或版本。
- 部署模式。
- 复现步骤。
- 影响范围和已知缓解方式。

## Admin Token

`NAVIGATION_ADMIN_TOKEN` 用于保护后台 API。请使用足够长的随机字符串，并确保它不会提交到仓库中。登录后台后，浏览器会把 Token 保存在 `localStorage`，因此不要在共享设备上使用后台。

## 存储注意事项

- Vercel Blob 部署写入数据时需要 `BLOB_READ_WRITE_TOKEN`。
- 本地文件部署需要挂载可写的数据目录。
- 只读配置模式会有意禁用后台保存。
